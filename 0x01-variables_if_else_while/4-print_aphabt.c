#include<stdio.h>

/**
 * main - print alphabet except q and e
 * Return: 0 return always 0,otherwise 1
 */

int main(void)
{
int letter = 'a';

while (letter <= 'z')
while (letter != 'e' && 'p')
{
putchar(letter);
letter++;
}
putchar('\n');
return (0);

}
