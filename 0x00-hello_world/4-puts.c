#include<stdio.h>
/**
* main - prints the phrase in the variable
* Return: 0 if exists properly, non-zero otherwise
*/
int main(void)
{
char variable[] = "\"Programming is like building a multilingual puzzle";
puts(variable);
return (0);
}
