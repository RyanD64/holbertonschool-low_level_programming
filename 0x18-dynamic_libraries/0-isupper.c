#include <stdio.h>
#include"main.h"

/**
 * _isupper - tell if character is upper
 * @c: print a random character
 * Return: 0 otherwise 1
 */

int _isupper(int c)
{
if ((c >= 65) && (c <= 90))
{
return (1);
}
else
{
return (0);

}
}

