#include"main.h"
/**
 * _isdigit - tells if character is a digital number
 * @c: prints a random character
 * Return: 0 otherwise 1
 */

int _isdigit(int c)
{
if (c >= 48 && c <= 57)
{
return (1);
}
else
{
return (0);
}
}
